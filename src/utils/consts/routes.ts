export const Routes = {
  Home: '/',
  /** Страница объекта. Slug задаётся в админке и живёт в рекламе —
   *  собираем ссылку одной функцией, чтобы адрес не расползался по коду. */
  Object: (slug: string) => `/object/${slug}`,
  /** Правовые страницы. Ими же по умолчанию заполнены «Правовые документы»
   *  в админке — там в поле ссылки достаточно написать путь. */
  Privacy: '/privacy',
  Consent: '/consent',
} as const;
